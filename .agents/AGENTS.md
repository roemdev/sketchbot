# Project Rules

## Administrative Commands
- All administrative and moderator commands (e.g., `/sobres regalar`) must return simple, plain-text confirmation messages.
- Do not use `ContainerBuilder` panels, titles, or footers for administrative or system confirmation replies.
